#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Portuguese");
    int n=10,i;
    int v[n];
    for(i=0;i<n;i++)
	{
		v[i];
	}
	for(i=0;i<n;i++)
	{
		printf("%d ",v[i]);
	}
    return 0;

}

